<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Web Application</title>
    <link rel="stylesheet" href="/styles/layout.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
</head>

<form action="<%= locals.isEdit ? `/edit/${locals.blogDetails?.id}` : '/' %>"  method="post">
    <div class="compose">
    <h2><%= locals.isEdit ? 'Edit Blog' : 'Compose Blog' %></h2>
    <input type="text" name="title" class="title" placeholder="Title" value="<%= locals.isEdit ? `${locals.blogDetails?.title}` : '' %>" required>
    <textarea type="text" name="content"placeholder="Write Here ...." class="content" required><%= locals.isEdit ? `${locals.blogDetails?.description}` : '' %></textarea>
    <input type="submit" class="pub-btn" value="<%= locals.isEdit ? 'Edit' : 'Publish' %>">
</form>
 </div>
</html>

